import { UnderConstruction } from "@mou7asib/ui";

export default function ParametresPage() {
  return (
    <UnderConstruction
      title="Paramètres"
      note="Plan comptable, taux effectifs, utilisateurs et connecteurs restent à construire. Voir la feuille de route, phase 16."
    />
  );
}
